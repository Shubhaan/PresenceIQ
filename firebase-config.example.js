// Copy these values into firebase-config.js and fill in your real Firebase project values.
// PresenceIQ only uses Firebase Auth in this version.

window.PRESENCE_IQ_FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  appId: "YOUR_APP_ID",
};

window.PRESENCE_IQ_APP_CONFIG = {
  aiApiBaseUrl: "",
  aiApiKey: "YOUR_PUBLIC_APP_KEY",
};
